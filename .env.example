# ===========================================
# Environment Configuration
# ===========================================
# Copy this file to .env.local and fill in your values

# -------------------------------------------
# Core Settings (Required)
# -------------------------------------------

# Environment: development | production | test
NODE_ENV=development

# PostgreSQL database connection string
DATABASE_URL=postgresql://user:password@localhost:5432/database_name

# Secret key for Payload CMS authentication (generate a random string)
PAYLOAD_SECRET=your-super-secret-key-here

# -------------------------------------------
# Public URLs (Client-accessible)
# -------------------------------------------

# Base URL of your server (used for emails and API calls)
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Public site URL (optional, defaults to VERCEL_URL in production)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# -------------------------------------------
# Mapbox Configuration
# -------------------------------------------

# Mapbox access token for maps and geocoding
# Get yours at: https://account.mapbox.com/access-tokens/
NEXT_PUBLIC_MAPBOX_TOKEN=pk.your-mapbox-token-here
# Alternative name (both work):
# NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=pk.your-mapbox-token-here

# -------------------------------------------
# Google Maps Configuration (Optional)
# -------------------------------------------

# Google Maps API key for routing/directions
# Get yours at: https://console.cloud.google.com/apis/credentials
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# -------------------------------------------
# Redis / Caching (Choose one option)
# -------------------------------------------

# Option 1: Upstash Redis (recommended for Vercel)
UPSTASH_REDIS_REST_URL=https://your-upstash-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-upstash-token

# Option 2: Vercel KV (alternative to Upstash)
# KV_REST_API_URL=https://your-kv-url.vercel-storage.com
# KV_REST_API_TOKEN=your-kv-token

# Option 3: Local Redis (for local development)
# REDIS_URL=redis://localhost:6379

# -------------------------------------------
# Email Configuration (Choose one option)
# -------------------------------------------

# Option 1: Production SMTP
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password

# Option 2: Ethereal (for development/testing)
# Create account at: https://ethereal.email/
# ETHEREAL_USER=your-ethereal-user@ethereal.email
# ETHEREAL_PASS=your-ethereal-password

# Sender configuration
EMAIL_FROM=noreply@example.com
EMAIL_FROM_NAME=Your App Name

# -------------------------------------------
# API Security
# -------------------------------------------

# API key for scraper endpoints (generate a random string)
SCRAPER_API_KEY=your-scraper-api-key

# Secret for Vercel cron job authentication
# Must match the value in vercel.json cron configuration
CRON_SECRET=your-cron-secret

# -------------------------------------------
# Logging
# -------------------------------------------

# Log level: debug | info | warn | error
LOG_LEVEL=info

# -------------------------------------------
# AWS S3 Storage (Optional - for production file uploads)
# -------------------------------------------

# Leave these empty for local development (files stored locally)
# Set all four to enable S3 storage in production

# AWS credentials (use IAM user with S3 access)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# S3 bucket configuration
AWS_REGION=us-east-1
AWS_S3_BUCKET_NAME=your-bucket-name

# -------------------------------------------
# Auto-set by Platform (Do not set manually)
# -------------------------------------------

# VERCEL_URL - Automatically set by Vercel
# npm_package_version - Automatically set by Node.js
